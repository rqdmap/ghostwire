[general]
timezone = "Asia/Shanghai"

[activitywatch]
base_url = "http://127.0.0.1:5600"
timeout_seconds = 30
hosts = ["auto"]

[upload]
# server_url = "https://telemetry.example.com"

[categorize.terminal]
allow = [
  # macOS / Linux
  "Alacritty", "iTerm2", "kitty", "Neovim", "Code", "WezTerm", "Terminal", "Hyper",
  # Windows
  "Windows Terminal", "pwsh.exe", "powershell.exe", "cmd.exe",
]

[categorize.browser]
allow = [
  # macOS / Linux
  "Google Chrome", "Chromium", "Firefox", "Safari", "Arc", "Brave Browser",
  # Windows
  "Microsoft Edge", "msedge.exe", "chrome.exe", "firefox.exe",
]

[host_meta.work-mac]
label = "工作机"
platform = "macOS"

[host_meta.home-arch]
label = "家里机"
platform = "Arch Linux"

[host_meta.home-win]
label = "Windows机"
platform = "Windows"
