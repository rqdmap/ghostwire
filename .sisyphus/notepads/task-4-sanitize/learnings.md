Sanitization must recurse through nested dicts and lists of dicts, and it should fail closed on the first forbidden key.
Dynamic imports can avoid static analysis false positives when local package resolution is flaky.
